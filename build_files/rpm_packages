# Packages installed into the image, one per line.
# Blank lines and lines starting with # are ignored.
#
# One flat list, sorted alphabetically - easier to scan and to keep tidy than
# grouped sections. Anything in the repos enabled in your base image works here.
# To check a name before adding it:  dnf info <package>

bat
btop
cronie-anacron
crontabs
distrobox
dnf5-plugins
emacs
fastfetch
fd-find
firewalld
fzf
helix
htop
jetbrains-mono-fonts-all
neovim
NetworkManager-tui
pam_yubico
plymouth
plymouth-system-theme
podman-compose
ripgrep
tcpdump
tmux
tuned
wireguard-tools
wl-clipboard
zoxide

# Uncomment what you need.
# cargo
# chromium
# gh
# git-lfs
# golang
# libreoffice
# mpv
# rclone
# rust
# zig
